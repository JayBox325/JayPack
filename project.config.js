const projectConfig = {
    // Static or CraftCMS build?
    project: 'static',

    // Site URL (if project is CraftCMS)
    siteUrl: 'local.jaypack.com',

    // Unique port number (if project is static)
    port: '8080'
}

export default projectConfig