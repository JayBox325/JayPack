const projectConfig = {
    
    // CMS or static site. True or false.
    cms: false,

    // Custom domain for localhost if on a Craft project (Confirmed this works with MAMP so far)
    domain: 'local.jaypack.com',

    // Project structure
    distRoot: 'build', // Where everything is output
    srcRoot: '_src' // Source files
}

export default projectConfig